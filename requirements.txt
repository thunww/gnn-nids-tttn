# Lưu ý: trên Colab, torch đã cài sẵn kèm đúng bản CUDA — KHÔNG pip install torch ở đó,
# chỉ cần `pip install -r requirements.txt` để lấy các thư viện còn lại.
# Local (không GPU) thì cài torch bản CPU riêng trước: https://pytorch.org/get-started/locally/

torch_geometric>=2.5

# Baseline & xử lý dữ liệu
scikit-learn>=1.4
xgboost>=2.0
pandas>=2.2
numpy>=1.26
pyarrow>=15.0
networkx>=3.2

# Tracking & tối ưu siêu tham số
mlflow>=2.11
optuna>=3.6

# Đánh giá thống kê
statsmodels>=0.14

# Trực quan hoá
matplotlib>=3.8
seaborn>=0.13

# Kiểm thử
pytest>=8.0

# API demo (giai đoạn 5)
fastapi>=0.110
uvicorn>=0.29
