# results

Bảng/biểu đồ kết quả thực nghiệm đã tổng hợp (không phải raw MLflow logs), dùng trực tiếp cho báo cáo (`Nhom_A05/`) — mỗi hình/bảng nên khớp với số liệu trong `docs/phases/phase4_experiments_xai.md`.
