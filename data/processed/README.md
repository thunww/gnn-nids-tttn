# data/processed

Dữ liệu sau ETL (Parquet), đã làm sạch/chuẩn hoá/chia 70-15-15, và đồ thị đã build từ `src/graph`. Không sửa tay — luôn sinh lại từ `data/raw/` qua `src/etl` và `src/graph` để đảm bảo tái lập được.
